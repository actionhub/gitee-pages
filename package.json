{
  "name": "auto-refresh-gitee-pages",
  "version": "1.0.0",
  "description": "自动刷新gitee pages服务",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc && ncc build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.1.3",
    "@vercel/ncc": "^0.26.1"
  },
  "dependencies": {
    "@actions/core": "^1.2.6"
  }
}
